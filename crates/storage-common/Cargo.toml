[package]
name = "storage-common"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Shared helpers for storage backends (Redis SCAN, etc.)"

[dependencies]
redis = { workspace = true }
